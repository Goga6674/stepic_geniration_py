"""Напишите программу, которая умеет шифровать и расшифровывать шифр подстановки. Программа принимает на вход две строки одинаковой длины, на первой строке записаны символы исходного алфавита, на второй строке — символы конечного алфавита, после чего идёт строка, которую нужно зашифровать переданным ключом, и ещё одна строка, которую нужно расшифровать."""


# читаем строки с ввода преобразуем в список 
def cin():
    ls = []
    for i in range(4):
        ls.append(input())

    return ls
    
data = cin()		#глобальная переменная для храниения ввода    
# создаём 1 словарь на основании ключа - значения (шифра) и 2 словарь на основании значения - ключа(шифра)
def make_dikt(lst):
    dik_1 = {}
    dik_2 = {}
    out = [dik_1, dik_2]
    for i in range(len(lst[0])):
        dik_1.setdefault(lst[0][i], lst[1][i])
        dik_2.setdefault(lst[1][i], lst[0][i])
    return out

# шифруем слово 
def mk_sh(data, lst_d):
    sh = ''
    for ch in data[2]:
        sh += lst_d[0].get(ch)
    print(sh)
    
# расшифровываем слово
def un_sh(data, lst_d):
    un_sh = ''
    for ch in data[3]:
        un_sh += lst_d[1].get(ch)
    print(un_sh)

# вызов

mk_sh(data, make_dikt(data))
un_sh(data, make_dikt(data))
